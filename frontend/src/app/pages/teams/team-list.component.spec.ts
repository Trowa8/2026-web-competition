import { TeamListComponent } from './team-list.component';

describe('TeamListComponent', () => {
    it('should create', () => {
        expect(new TeamListComponent()).toBeTruthy();
    });
});