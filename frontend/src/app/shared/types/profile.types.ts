export type TournamentHistory = {
    id: number;
    name: string;
    date: string;
    place: number | null;
    result: string;
};

export type TeamMember = {
    id: number;
    name: string;
};
