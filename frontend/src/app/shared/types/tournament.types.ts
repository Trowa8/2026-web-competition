export type Tournament = {
    tournament_id: number;
    name: string;
    description?: string;
    start_date: string;
    registration_deadline: string;
    created_by: number;
    created_at: string;
};